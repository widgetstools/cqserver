[package]
name = "cq-differential-tests"
version.workspace = true
edition.workspace = true

[dependencies]
cq-core = { path = "../cq-core" }
serde.workspace = true
serde_json.workspace = true
serde_yaml = { workspace = true }
anyhow.workspace = true
tokio = { workspace = true }

# DataFusion reference engine. Pure Rust, no C toolchain required —
# builds clean on Windows alongside everything else. Arrow types are
# exposed directly so we can build RecordBatches without going through
# DataFusion's own re-export gymnastics.
datafusion = { workspace = true }
arrow = { workspace = true }

[dev-dependencies]
